<div class="container">
  <div class="row">
    <div class="m-md-5 d-inline-block">
      <p><%= image_tag @game.image, size:"300x200" %></p>
    </div>
    <div class="m-md-5">
      <strong><%= @game.name %></strong>
      <p class="mt-md-3"><%= @game.introduction %></p>
      <%# @game.tags.each do |tag| %>
      <%#= tag.name %>
      <%# end %>
      <%= render "public/game_comments/index", game: @game, game_comments: @game_comments %>
      <%= render "public/game_comments/form", game: @game %>
    </div>
  </div>
</div>